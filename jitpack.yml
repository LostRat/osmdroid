jdk:
  - openjdk17   # JitPack supports 8, 11, 17. Use 17 for modern Android builds.

before_install:
  - ./gradlew clean --no-daemon

install:
  - ./gradlew build publishToMavenLocal -x test --no-daemon
